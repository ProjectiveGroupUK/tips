INSERT INTO tips_md_schema.process_cmd (
    {% for param_key, param_value in parameters.items() %}
        {% if param_value is not none %}
            {% if not loop.first %}, {% endif %}
            {{param_key}}
        {% endif %}
    {% endfor %}
)
    {% for param_key, param_value in parameters.items() %}
        {% if param_value is not none %}
            {% if not loop.first %}, {% endif %}
            {{param_value}}
        {% endif %}
    {% endfor %}
FROM tips_md_schema.process_cmd
WHERE process_id = {{parameters.PROCESS_ID}};